import React, { useState } from "react";
import { Link2, Plus } from "lucide-react";
import { PayType, Project, ProjectWorkerAssignment, Worker } from "../../types";
import { createLocalAssignment } from "../../lib/payroll";

interface ProjectAssignmentsProps { assignments: ProjectWorkerAssignment[]; workers: Worker[]; projects: Project[]; onSave: (assignment: ProjectWorkerAssignment) => void; }

export const ProjectAssignments: React.FC<ProjectAssignmentsProps> = ({ assignments, workers, projects, onSave }) => {
  const [editing, setEditing] = useState<ProjectWorkerAssignment | null>(null);
  const open = () => setEditing(createLocalAssignment({ workerId: workers[0]?.id || "", projectId: projects.find((project) => project.status !== "ARCHIVED")?.id || "", startDate: new Date().toISOString().slice(0, 10), active: true, roleOnProject: "", notes: "" }));
  const save = (event: React.FormEvent) => { event.preventDefault(); if (!editing?.workerId || !editing.projectId || !editing.startDate) return; onSave(editing); setEditing(null); };
  return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between gap-3 border-b border-slate-100 p-5"><div><h3 className="text-sm font-black">Project assignments</h3><p className="mt-1 text-xs text-slate-500">A worker can be active on more than one project over time.</p></div><button onClick={open} disabled={!workers.length || !projects.length} className="inline-flex items-center gap-1.5 rounded-xl bg-indigo-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-50"><Plus className="h-3.5 w-3.5" /> Assign worker</button></div>{assignments.length ? <div className="divide-y divide-slate-100">{assignments.map((assignment) => { const worker = workers.find((item) => item.id === assignment.workerId); const project = projects.find((item) => item.id === assignment.projectId); return <div key={assignment.id} className="flex items-center justify-between gap-3 px-5 py-4"><div><p className="text-xs font-black">{worker?.displayName || "Worker"} <span className="font-normal text-slate-400">→</span> {project?.projectCode || "Project"}</p><p className="mt-1 text-[10px] text-slate-500">{assignment.roleOnProject || worker?.jobTitle || "Role not set"} • since {assignment.startDate}{assignment.endDate ? ` → ${assignment.endDate}` : ""}</p></div><span className="text-[10px] font-bold text-emerald-700">{assignment.active ? "Active" : "Inactive"}</span></div>; })}</div> : <div className="p-10 text-center"><Link2 className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 text-sm font-bold text-slate-700">No project assignments yet.</p><p className="mt-1 text-xs text-slate-500">Assign workers to connect time and payroll costs to projects.</p></div>}{editing && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={save} className="w-full max-w-lg space-y-4 rounded-2xl bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Assign worker to project</h3><button type="button" onClick={() => setEditing(null)} className="text-xl text-slate-400">×</button></div><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Worker</span><select value={editing.workerId} onChange={(event) => setEditing({ ...editing, workerId: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs">{workers.map((worker) => <option key={worker.id} value={worker.id}>{worker.displayName}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Project</span><select value={editing.projectId} onChange={(event) => setEditing({ ...editing, projectId: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs">{projects.filter((project) => project.status !== "ARCHIVED").map((project) => <option key={project.id} value={project.id}>{project.projectCode} — {project.projectName}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Start date</span><input type="date" value={editing.startDate} onChange={(event) => setEditing({ ...editing, startDate: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs" /></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">End date</span><input type="date" value={editing.endDate || ""} onChange={(event) => setEditing({ ...editing, endDate: event.target.value || undefined })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs" /></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Role on project</span><input value={editing.roleOnProject || ""} onChange={(event) => setEditing({ ...editing, roleOnProject: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs" /></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Pay type override</span><select value={editing.payType || ""} onChange={(event) => setEditing({ ...editing, payType: (event.target.value || undefined) as PayType | undefined })} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-xs"><option value="">Worker default</option>{["MONTHLY", "DAILY", "HOURLY"].map((value) => <option key={value} value={value}>{value}</option>)}</select></label></div><div className="flex justify-end gap-2"><button type="button" onClick={() => setEditing(null)} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold">Cancel</button><button type="submit" className="rounded-xl bg-indigo-600 px-4 py-2 text-xs font-bold text-white">Save assignment</button></div></form></div>}</section>;
};

