import React, { useState } from "react";
import { CalendarDays, Plus } from "lucide-react";
import { PayrollPeriod, PayrollPeriodStatus } from "../../types";
import type { PayrollSchedule } from "../../lib/payrollSchedule";
import { payrollPeriodFrequencyLabel } from "../../lib/payrollIntegrity";
import { createLocalPeriod } from "../../lib/payroll";

interface PayrollPeriodsProps { periods: PayrollPeriod[]; schedules?: readonly PayrollSchedule[]; onSave: (period: PayrollPeriod) => void; }

export const PayrollPeriods: React.FC<PayrollPeriodsProps> = ({ periods, schedules, onSave }) => {
  const [editing, setEditing] = useState<PayrollPeriod | null>(null);
  const save = (event: React.FormEvent) => { event.preventDefault(); if (!editing?.periodStart || !editing.periodEnd) return; onSave(editing); setEditing(null); };
  return <section className="bg-white border border-slate-200 rounded-2xl shadow-sm overflow-hidden"><div className="p-5 border-b border-slate-100 flex items-center justify-between gap-3"><div><h3 className="text-sm font-black">Payroll periods</h3><p className="text-xs text-slate-500 mt-1">Manual periods keep the first payroll foundation transparent and reviewable.</p></div><button onClick={() => setEditing(createLocalPeriod({ periodStart: new Date().toISOString().slice(0, 10), periodEnd: new Date().toISOString().slice(0, 10), status: "DRAFT", notes: "" }))} className="inline-flex items-center gap-1.5 px-3 py-2 rounded-xl bg-indigo-600 text-white text-xs font-bold"><Plus className="w-3.5 h-3.5" /> New period</button></div>{periods.length ? <div className="divide-y divide-slate-100">{periods.map((period) => <div key={period.id} className="px-5 py-4 flex items-center justify-between gap-3"><div className="flex items-center gap-3"><CalendarDays className="w-4 h-4 text-indigo-600" /><div><p className="text-xs font-bold">{period.periodStart} – {period.periodEnd}</p><p className="text-[10px] text-slate-500">{schedules ? payrollPeriodFrequencyLabel(period, schedules) : "Payroll period"} · Pay date {period.payDate || "not set"}</p></div></div><button onClick={() => setEditing(period)} className="text-[10px] text-indigo-700 font-bold">{period.status}</button></div>)}</div> : <div className="p-10 text-center"><p className="text-sm font-bold text-slate-700">No payroll periods yet.</p><p className="text-xs text-slate-500 mt-1">Create Aug 1–15 or Aug 16–31 periods as needed.</p></div>}{editing && <div className="fixed inset-0 z-50 bg-slate-950/40 p-4 flex items-center justify-center"><form onSubmit={save} className="w-full max-w-md bg-white rounded-2xl shadow-2xl p-5 space-y-4"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Payroll period</h3><button type="button" onClick={() => setEditing(null)} className="text-xl text-slate-400">×</button></div><label className="space-y-1 block"><span className="text-[10px] uppercase font-black text-slate-500">Period start</span><input type="date" required value={editing.periodStart} onChange={(event) => setEditing({ ...editing, periodStart: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1 block"><span className="text-[10px] uppercase font-black text-slate-500">Period end</span><input type="date" required value={editing.periodEnd} onChange={(event) => setEditing({ ...editing, periodEnd: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1 block"><span className="text-[10px] uppercase font-black text-slate-500">Pay date</span><input type="date" value={editing.payDate || ""} onChange={(event) => setEditing({ ...editing, payDate: event.target.value || undefined })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1 block"><span className="text-[10px] uppercase font-black text-slate-500">Status</span><select value={editing.status} onChange={(event) => setEditing({ ...editing, status: event.target.value as PayrollPeriodStatus })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs">{["DRAFT", "OPEN", "CALCULATED", "APPROVED", "PAID", "VOID"].map((value) => <option key={value} value={value}>{value}</option>)}</select></label><div className="flex justify-end gap-2"><button type="button" onClick={() => setEditing(null)} className="px-3 py-2 rounded-xl border border-slate-200 text-xs font-bold">Cancel</button><button type="submit" className="px-4 py-2 rounded-xl bg-indigo-600 text-white text-xs font-bold">Save period</button></div></form></div>}</section>;
};

