import React, { useState } from "react";
import { Plus, Repeat2, Save, UserRound } from "lucide-react";
import type { Project, Worker } from "../../types";
import { LABOR_CONTEXTS, type RecurringPayrollComponent, type WorkerCompensationProfile } from "../../lib/payrollAutomation";

interface PayrollProfilesProps {
  workers: Worker[];
  projects: Project[];
  profiles: WorkerCompensationProfile[];
  components: RecurringPayrollComponent[];
  onSaveProfile: (profile: WorkerCompensationProfile) => void;
  onSaveComponent: (component: RecurringPayrollComponent) => void;
}

const id = (prefix: string) => globalThis.crypto?.randomUUID?.() || `local-${prefix}-${Date.now()}`;
const today = () => new Date().toISOString().slice(0, 10);

function profileSeed(worker?: Worker): WorkerCompensationProfile {
  return { id: id("profile"), workerId: worker?.id || "", effectiveFrom: today(), frequency: worker?.defaultPayType || "MONTHLY", rate: worker?.defaultRate || 0, defaultLaborContext: "UNALLOCATED_REVIEW" };
}
function componentSeed(worker?: Worker): RecurringPayrollComponent {
  return { id: id("component"), workerId: worker?.id || "", type: "EARNING", name: "", amount: 0, effectiveFrom: today(), active: true };
}

export const PayrollProfiles: React.FC<PayrollProfilesProps> = ({ workers, projects, profiles, components, onSaveProfile, onSaveComponent }) => {
  const [profile, setProfile] = useState<WorkerCompensationProfile | null>(null);
  const [component, setComponent] = useState<RecurringPayrollComponent | null>(null);
  const saveProfile = (event: React.FormEvent) => { event.preventDefault(); if (!profile?.workerId || !profile.effectiveFrom || profile.rate < 0) return; onSaveProfile(profile); setProfile(null); };
  const saveComponent = (event: React.FormEvent) => { event.preventDefault(); if (!component?.workerId || !component.name?.trim() || !component.effectiveFrom) return; onSaveComponent({ ...component, amount: component.rate !== undefined ? undefined : Number(component.amount) || 0, rate: component.rate !== undefined ? Number(component.rate) || 0 : undefined }); setComponent(null); };
  const workerName = (workerId: string) => workers.find((worker) => worker.id === workerId)?.displayName || "Unknown worker";
  return <section className="grid gap-4 lg:grid-cols-2">
    <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between gap-3 border-b border-slate-100 p-5"><div><p className="text-[10px] font-black uppercase tracking-[0.16em] text-indigo-600">Worker setup</p><h3 className="mt-1 text-sm font-black">Effective compensation</h3><p className="mt-1 text-xs text-slate-500">Historical rates remain attached to their effective dates.</p></div><button type="button" disabled={!workers.length} onClick={() => setProfile(profileSeed(workers[0]))} className="inline-flex items-center gap-1.5 rounded-xl bg-indigo-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-40"><Plus className="h-3.5 w-3.5" /> Add profile</button></div>{profiles.length ? <div className="divide-y divide-slate-100">{profiles.map((item) => <div key={item.id} className="flex items-center justify-between gap-3 px-5 py-3"><div className="flex min-w-0 items-center gap-2"><UserRound className="h-4 w-4 shrink-0 text-indigo-600" /><div className="min-w-0"><p className="truncate text-xs font-black">{workerName(item.workerId)} · {item.frequency}</p><p className="text-[10px] text-slate-500">₱{item.rate.toLocaleString("en-PH")} from {String(item.effectiveFrom).slice(0, 10)} · {item.defaultLaborContext.replace("_", " ")}</p></div></div><span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-bold text-emerald-700">Effective</span></div>)}</div> : <div className="p-8 text-center text-xs text-slate-500">No separate profiles yet. Payroll falls back to the worker’s current default rate.</div>}</div>
    <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between gap-3 border-b border-slate-100 p-5"><div><p className="text-[10px] font-black uppercase tracking-[0.16em] text-violet-600">Recurring setup</p><h3 className="mt-1 text-sm font-black">Payroll components</h3><p className="mt-1 text-xs text-slate-500">Allowances, authorized deductions, and future employer costs.</p></div><button type="button" disabled={!workers.length} onClick={() => setComponent(componentSeed(workers[0]))} className="inline-flex items-center gap-1.5 rounded-xl bg-violet-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-40"><Repeat2 className="h-3.5 w-3.5" /> Add component</button></div>{components.length ? <div className="divide-y divide-slate-100">{components.map((item) => <div key={item.id} className="flex items-center justify-between gap-3 px-5 py-3"><div className="min-w-0"><p className="truncate text-xs font-black">{workerName(item.workerId)} · {item.name}</p><p className="text-[10px] text-slate-500">{item.type} · {item.amount !== undefined ? `₱${item.amount.toLocaleString("en-PH")}` : `${((item.rate || 0) * 100).toFixed(2)}%`} from {String(item.effectiveFrom).slice(0, 10)}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${item.active ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-500"}`}>{item.active ? "Active" : "Off"}</span></div>)}</div> : <div className="p-8 text-center text-xs text-slate-500">No recurring components configured.</div>}</div>
    {profile && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={saveProfile} className="w-full max-w-lg space-y-4 rounded-2xl bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Compensation profile</h3><button type="button" onClick={() => setProfile(null)} className="text-xl text-slate-400">×</button></div><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1"><span className="field-label">Worker</span><select required value={profile.workerId} onChange={(event) => setProfile({ ...profile, workerId: event.target.value })} className="field-input">{workers.map((worker) => <option key={worker.id} value={worker.id}>{worker.displayName}</option>)}</select></label><label className="space-y-1"><span className="field-label">Pay type</span><select value={profile.frequency} onChange={(event) => setProfile({ ...profile, frequency: event.target.value as WorkerCompensationProfile["frequency"] })} className="field-input"><option value="MONTHLY">Monthly</option><option value="DAILY">Daily</option><option value="HOURLY">Hourly</option></select></label><label className="space-y-1"><span className="field-label">Rate</span><input required min="0" step="0.01" type="number" value={profile.rate} onChange={(event) => setProfile({ ...profile, rate: Number(event.target.value) })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Effective from</span><input required type="date" value={String(profile.effectiveFrom).slice(0, 10)} onChange={(event) => setProfile({ ...profile, effectiveFrom: event.target.value })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Effective to</span><input type="date" value={profile.effectiveTo ? String(profile.effectiveTo).slice(0, 10) : ""} onChange={(event) => setProfile({ ...profile, effectiveTo: event.target.value || undefined })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Labor context</span><select value={profile.defaultLaborContext} onChange={(event) => setProfile({ ...profile, defaultLaborContext: event.target.value as WorkerCompensationProfile["defaultLaborContext"] })} className="field-input">{LABOR_CONTEXTS.map((context) => <option key={context} value={context}>{context.replace("_", " ")}</option>)}</select></label><label className="space-y-1 sm:col-span-2"><span className="field-label">Default project (convenience only)</span><select value={profile.defaultProjectId || ""} onChange={(event) => setProfile({ ...profile, defaultProjectId: event.target.value || undefined })} className="field-input"><option value="">No default project</option>{projects.filter((project) => project.status !== "ARCHIVED").map((project) => <option key={project.id} value={project.id}>{project.projectCode} · {project.projectName}</option>)}</select></label></div><div className="flex justify-end"><button type="submit" className="inline-flex items-center gap-1.5 rounded-xl bg-indigo-600 px-4 py-2 text-xs font-bold text-white"><Save className="h-3.5 w-3.5" /> Save profile</button></div></form></div>}
    {component && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={saveComponent} className="w-full max-w-lg space-y-4 rounded-2xl bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Recurring payroll component</h3><button type="button" onClick={() => setComponent(null)} className="text-xl text-slate-400">×</button></div><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1"><span className="field-label">Worker</span><select required value={component.workerId} onChange={(event) => setComponent({ ...component, workerId: event.target.value })} className="field-input">{workers.map((worker) => <option key={worker.id} value={worker.id}>{worker.displayName}</option>)}</select></label><label className="space-y-1"><span className="field-label">Type</span><select value={component.type} onChange={(event) => setComponent({ ...component, type: event.target.value as RecurringPayrollComponent["type"] })} className="field-input"><option value="EARNING">Earning</option><option value="DEDUCTION">Deduction</option><option value="EMPLOYER_COST">Employer cost</option></select></label><label className="space-y-1 sm:col-span-2"><span className="field-label">Name</span><input required value={component.name || ""} onChange={(event) => setComponent({ ...component, name: event.target.value })} className="field-input" placeholder="Transportation allowance" /></label><label className="space-y-1"><span className="field-label">Amount</span><input type="number" min="0" step="0.01" value={component.amount ?? ""} onChange={(event) => setComponent({ ...component, amount: Number(event.target.value), rate: undefined })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Or rate (0.1 = 10%)</span><input type="number" min="0" step="0.001" value={component.rate ?? ""} onChange={(event) => setComponent({ ...component, rate: Number(event.target.value), amount: undefined })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Effective from</span><input required type="date" value={String(component.effectiveFrom).slice(0, 10)} onChange={(event) => setComponent({ ...component, effectiveFrom: event.target.value })} className="field-input" /></label><label className="space-y-1"><span className="field-label">Effective to</span><input type="date" value={component.effectiveTo ? String(component.effectiveTo).slice(0, 10) : ""} onChange={(event) => setComponent({ ...component, effectiveTo: event.target.value || undefined })} className="field-input" /></label></div><div className="flex justify-end"><button type="submit" className="inline-flex items-center gap-1.5 rounded-xl bg-violet-600 px-4 py-2 text-xs font-bold text-white"><Save className="h-3.5 w-3.5" /> Save component</button></div></form></div>}
  </section>;
};
