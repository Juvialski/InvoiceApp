import React, { useState } from "react";
import { Expense, ExpenseStatus, Project } from "../../types";
import { EXPENSE_CATEGORIES, createLocalExpense } from "../../lib/expenses";

interface ExpenseFormProps {
  projects: Project[];
  initial?: Expense;
  projectId?: string;
  onSave: (expense: Expense) => void;
  onCancel: () => void;
}

export const ExpenseForm: React.FC<ExpenseFormProps> = ({ projects, initial, projectId, onSave, onCancel }) => {
  const [expense, setExpense] = useState<Expense>(initial || createLocalExpense({ projectId, expenseDate: new Date().toISOString().slice(0, 10), category: "Miscellaneous", description: "", payee: "", amount: 0, currency: "PHP", paymentMethod: "", referenceNumber: "", status: "DRAFT", notes: "" }));
  const update = (patch: Partial<Expense>) => setExpense((current) => ({ ...current, ...patch }));
  return <form onSubmit={(event) => { event.preventDefault(); if (!expense.description.trim() || expense.amount < 0) return; onSave({ ...expense, description: expense.description.trim(), category: expense.category.trim() || "Miscellaneous", currency: expense.currency.toUpperCase(), amount: Number(expense.amount) || 0 }); }} className="space-y-4">
    <div className="grid sm:grid-cols-2 gap-3"><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Date</span><input type="date" value={expense.expenseDate} onChange={(event) => update({ expenseDate: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Project</span><select value={expense.projectId || ""} onChange={(event) => update({ projectId: event.target.value || undefined })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs"><option value="">Unallocated</option>{projects.filter((project) => project.status !== "ARCHIVED").map((project) => <option key={project.id} value={project.id}>{project.projectCode} — {project.projectName}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Category</span><input list="expense-categories" value={expense.category} onChange={(event) => update({ category: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /><datalist id="expense-categories">{EXPENSE_CATEGORIES.map((category) => <option key={category} value={category} />)}</datalist></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Amount</span><div className="flex"><input type="number" min="0" step="0.01" value={expense.amount} onChange={(event) => update({ amount: Number(event.target.value) })} className="min-w-0 flex-1 px-3 py-2 rounded-l-xl border border-slate-200 text-xs" /><input value={expense.currency} onChange={(event) => update({ currency: event.target.value })} className="w-20 px-2 py-2 rounded-r-xl border-y border-r border-slate-200 text-xs uppercase" /></div></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Payee</span><input value={expense.payee || ""} onChange={(event) => update({ payee: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1"><span className="text-[10px] font-black uppercase text-slate-500">Status</span><select value={expense.status} onChange={(event) => update({ status: event.target.value as ExpenseStatus })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs">{["DRAFT", "APPROVED", "PAID", "VOID"].map((value) => <option key={value} value={value}>{value}</option>)}</select></label><label className="space-y-1 sm:col-span-2"><span className="text-[10px] font-black uppercase text-slate-500">Description</span><input required value={expense.description} onChange={(event) => update({ description: event.target.value })} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label><label className="space-y-1 sm:col-span-2"><span className="text-[10px] font-black uppercase text-slate-500">Reference / notes</span><textarea value={expense.notes || ""} onChange={(event) => update({ notes: event.target.value, referenceNumber: event.target.value })} rows={2} className="w-full px-3 py-2 rounded-xl border border-slate-200 text-xs" /></label></div>
    <div className="flex justify-end gap-2"><button type="button" onClick={onCancel} className="px-3 py-2 rounded-xl border border-slate-200 text-xs font-bold">Cancel</button><button type="submit" className="px-4 py-2 rounded-xl bg-indigo-600 text-white text-xs font-bold">Save expense</button></div>
  </form>;
};

