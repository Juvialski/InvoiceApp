import React from "react";
import { Archive, Ban, Plus, Receipt } from "lucide-react";
import type { Expense } from "../../types";

interface ProjectExpensesProps { projectId: string; currency: string; expenses: Expense[]; onAdd?: () => void; onOpenCorrection?: (expense: Expense) => void; }
function money(value: number, currency: string) { try { return new Intl.NumberFormat("en-PH", { style: "currency", currency, maximumFractionDigits: 2 }).format(value || 0); } catch { return `${currency} ${(value || 0).toFixed(2)}`; } }

export const ProjectExpenses: React.FC<ProjectExpensesProps> = ({ currency, expenses, onAdd, onOpenCorrection }) => <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between gap-3 border-b border-slate-100 p-5"><div><h3 className="text-sm font-black">Project expenses</h3><p className="mt-1 text-xs text-slate-500">Direct costs stay separate from supplier invoices. Archive preserves cost; void removes active cost.</p></div>{onAdd && <button onClick={onAdd} className="inline-flex items-center gap-1.5 rounded-xl bg-indigo-600 px-3 py-2 text-xs font-bold text-white"><Plus className="h-3.5 w-3.5" /> Add expense</button>}</div>{expenses.length ? <div className="divide-y divide-slate-100">{expenses.map((expense) => <div key={expense.id} className="flex items-center justify-between gap-3 px-5 py-4"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="truncate text-xs font-bold">{expense.description}</p><span className={`rounded-full px-2 py-0.5 text-[9px] font-black ${expense.status === "VOID" ? "bg-slate-100 text-slate-600" : expense.status === "DRAFT" ? "bg-amber-50 text-amber-800" : "bg-emerald-50 text-emerald-800"}`}>{expense.status}</span>{expense.archivedAt && <span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2 py-0.5 text-[9px] font-black text-slate-600"><Archive className="h-3 w-3" /> Archived</span>}</div><p className="mt-1 text-[10px] text-slate-500">{expense.expenseDate} • {expense.category}{expense.payee ? ` • ${expense.payee}` : ""}</p></div><div className="flex shrink-0 items-center gap-2"><p className="font-sans text-xs font-black tabular-nums">{money(expense.amount, expense.currency || currency)}</p>{onOpenCorrection && <button onClick={() => onOpenCorrection(expense)} className="rounded-lg p-2 text-slate-400 hover:bg-amber-50 hover:text-amber-700" aria-label={`Review correction options for ${expense.description}`} title="Review correction options">{expense.status === "VOID" ? <Ban className="h-3.5 w-3.5" /> : <Archive className="h-3.5 w-3.5" />}</button>}</div></div>)}</div> : <div className="p-10 text-center"><Receipt className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 text-sm font-bold text-slate-700">No additional expenses recorded.</p><p className="mt-1 text-xs text-slate-500">Add fuel, transport, permits, meals, or other direct costs here.</p></div>}</section>;
