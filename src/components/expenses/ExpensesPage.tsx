import React, { useMemo, useState } from "react";
import { Plus, Search } from "lucide-react";
import { Expense, Project } from "../../types";
import { ExpenseForm } from "./ExpenseForm";

interface ExpensesPageProps { expenses: Expense[]; projects: Project[]; onSave: (expense: Expense) => void; onArchive: (expense: Expense) => void; initialProjectId?: string; }
function money(value: number, currency: string) { try { return new Intl.NumberFormat("en-PH", { style: "currency", currency, maximumFractionDigits: 2 }).format(value || 0); } catch { return `${currency} ${(value || 0).toFixed(2)}`; } }

export const ExpensesPage: React.FC<ExpensesPageProps> = ({ expenses, projects, onSave, onArchive, initialProjectId }) => {
  const [query, setQuery] = useState(""); const [status, setStatus] = useState("ALL"); const [modal, setModal] = useState(false);
  const rows = useMemo(() => expenses.filter((expense) => { const q = query.toLowerCase().trim(); const project = projects.find((item) => item.id === expense.projectId); const haystack = [expense.description, expense.category, expense.payee, expense.referenceNumber, project?.projectCode, project?.projectName].join(" ").toLowerCase(); return (!q || haystack.includes(q)) && (status === "ALL" || expense.status === status); }), [expenses, projects, query, status]);
  const confirmed = expenses.filter((expense) => expense.status === "APPROVED" || expense.status === "PAID").reduce((sum, expense) => sum + (expense.currency === "PHP" ? expense.amount : 0), 0);
  return <div className="space-y-5"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-3"><div><p className="text-[10px] uppercase tracking-[0.2em] font-black text-indigo-600">Project cost control</p><h2 className="text-xl sm:text-2xl font-black">Expenses</h2><p className="text-xs text-slate-500 mt-1">Direct expenses remain distinct from extracted supplier invoices.</p></div><button onClick={() => setModal(true)} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-indigo-600 text-white text-xs font-bold"><Plus className="w-3.5 h-3.5" /> Add expense</button></div><div className="grid grid-cols-2 md:grid-cols-4 gap-3"><div className="bg-white border border-slate-200 rounded-2xl p-4 shadow-sm"><p className="text-xl font-black">{expenses.length}</p><p className="text-[10px] text-slate-500 font-semibold">All expenses</p></div><div className="bg-white border border-slate-200 rounded-2xl p-4 shadow-sm"><p className="text-xl font-black text-emerald-700 font-sans tabular-nums">{money(confirmed, "PHP")}</p><p className="text-[10px] text-slate-500 font-semibold">Confirmed PHP cost</p></div><div className="bg-white border border-slate-200 rounded-2xl p-4 shadow-sm"><p className="text-xl font-black text-amber-700">{expenses.filter((expense) => expense.status === "DRAFT").length}</p><p className="text-[10px] text-slate-500 font-semibold">Draft / pending</p></div><div className="bg-white border border-slate-200 rounded-2xl p-4 shadow-sm"><p className="text-xl font-black text-slate-500">{expenses.filter((expense) => expense.status === "VOID").length}</p><p className="text-[10px] text-slate-500 font-semibold">Void</p></div></div><div className="bg-white border border-slate-200 rounded-2xl p-3 shadow-sm flex flex-col sm:flex-row gap-2"><label className="flex items-center gap-2 flex-1 px-3 py-2 rounded-xl bg-slate-50 border border-slate-200"><Search className="w-4 h-4 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search expense, project, payee…" className="w-full bg-transparent outline-none text-xs" /></label><select value={status} onChange={(event) => setStatus(event.target.value)} className="px-3 py-2 rounded-xl border border-slate-200 text-xs font-semibold"><option value="ALL">All statuses</option>{["DRAFT", "APPROVED", "PAID", "VOID"].map((value) => <option key={value} value={value}>{value}</option>)}</select></div><section className="bg-white border border-slate-200 rounded-2xl shadow-sm overflow-hidden">{rows.length ? <div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase text-slate-500"><tr><th className="px-5 py-3">Date / description</th><th className="px-5 py-3">Project</th><th className="px-5 py-3">Category</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Status</th><th className="px-5 py-3" /></tr></thead><tbody className="divide-y divide-slate-100">{rows.map((expense) => { const project = projects.find((item) => item.id === expense.projectId); return <tr key={expense.id}><td className="px-5 py-3"><p className="font-bold">{expense.description}</p><p className="text-[10px] text-slate-500">{expense.expenseDate}{expense.payee ? ` • ${expense.payee}` : ""}</p></td><td className="px-5 py-3 text-slate-600">{project ? `${project.projectCode} — ${project.projectName}` : "Unallocated"}</td><td className="px-5 py-3 text-slate-600">{expense.category}</td><td className="px-5 py-3 font-black font-sans tabular-nums">{money(expense.amount, expense.currency)}</td><td className="px-5 py-3"><span className="text-[10px] font-black">{expense.status}</span></td><td className="px-5 py-3 text-right"><button onClick={() => onArchive(expense)} className="text-[10px] font-bold text-rose-700">Archive</button></td></tr>; })}</tbody></table></div> : <div className="p-12 text-center"><p className="text-sm font-bold text-slate-700">No expenses yet.</p><p className="text-xs text-slate-500 mt-1">Add direct project costs without duplicating supplier invoices.</p></div>}</section>{modal && <div className="fixed inset-0 z-50 bg-slate-950/40 p-4 flex items-center justify-center"><div className="w-full max-w-2xl bg-white rounded-2xl shadow-2xl p-5"><div className="flex items-center justify-between mb-4"><h3 className="text-lg font-black">Add direct expense</h3><button onClick={() => setModal(false)} className="text-xl text-slate-400">×</button></div><ExpenseForm projects={projects} projectId={initialProjectId} onSave={(expense) => { onSave(expense); setModal(false); }} onCancel={() => setModal(false)} /></div></div>}</div>;
};
