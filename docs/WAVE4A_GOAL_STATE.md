# Wave 4A Goal State

- Branch: `hardening/wave4a-crud-rbac-audit`
- Base SHA: `93c1d32babfa0aae697dd2898f50dc9beabcd127` (refreshed `origin/main`)
- HEAD: current Wave 4A implementation plus checkpoint commits; implementation commit was `9503dd5f18e82abbefc458781de600cfbf5559c`
- Objective: Fix the Assistant composer submission regression; prove and fix the navigation/content flicker; audit shipped CRUD/RBAC workflows; document bounded gaps; validate, commit, push, and open (not merge) the Wave 4A PR.
- Completed: Repository freshness check; current `main` fetched; no open PRs; latest `main` CI runs for Wave 3 are green; WM-5 packets captured for `platform-shell`, `company-access-management`, `invoice-extraction-workflow`, `cash-accounts`, `worker-records`, `engineering-document`, `reports-screen`, and `assistant-screen`; Assistant composer fix; route-target stability fix; stale-while-revalidate presentation fix; bounded composite-permission/UI fixes; current CRUD/RBAC matrix.
- Confirmed navigation finding: `route` updated synchronously while the old `activeTab` was updated in a later effect, so AppRouter could briefly dispatch the previous route or `null`. AppRouter now derives its dispatch target directly from the canonical route. The navigation effect is not keyed by route and does not recreate the workspace sync controller or initiate a refresh.
- Confirmed sync finding: `App.tsx` already kept `workspaceLoading` false when any allowed group had usable cache data, and focus/visibility/realtime refreshes did not set it. `AppShell` nevertheless treated `connecting`/`syncing` as blocking pending state. That global presentation conflated initial hydration with background revalidation. It now blocks only `workspaceLoading`; the sidebar continues to show `Syncing`. Engineering/payroll group failure paths also preserve cached data/completeness state during background refresh.
- Confirmed Assistant finding: the composer previously cleared only after `sendMessage` resolved. It now performs the provider-equivalent synchronous acceptance check, snapshots the exact message, clears immediately, and leaves provider retry/history/attachment behavior intact. Gmail/project-context extraction paths use the same complete invoice permission bundle.
- CRUD/RBAC result: `docs/ENGORYX_CRUD_RBAC_MATRIX.md` records visible UI, effective permission, server/RPC/service, RLS/database, history, Assistant, seeded roles, custom GRANT/DENY, and suspended/revoked behavior. Composite actions now require all domain permissions; payroll import commit, invoice allocation/extraction, settlement targets, and read-only payroll/workforce controls are gated consistently.
- Validation to date: focused navigation/composer/workspace-sync/RBAC/Assistant/payroll/cash suite passed 89 tests; full `npm.cmd test` passed 752 tests with 1 expected live-DB test skipped; `npm.cmd run lint` passes after a minimal typed-token compatibility fix in `scripts/workflow-map/context.ts`; `npm.cmd run test:migrations` passed 65 static checks and skipped live upgrade replay because PostgreSQL at `127.0.0.1:54322` is unavailable; workflow-map generation/check/consistency and 59 workflow-map tests passed; production build passed with existing chunk-size/font/dynamic-import warnings. Browser local guest desktop/mobile route checks passed with no console errors or horizontal overflow.
- Files currently involved: `src/App.tsx`, `src/app/AppShell.tsx`, `src/app/routes/AppRouter.tsx`, `src/app/routes/CashBankingRoute.tsx`, `src/app/routes/InvoicesRoute.tsx`, `src/assistant/AssistantComposer.tsx`, `src/assistant/assistantComposerState.ts`, `src/components/EmailInbox.tsx`, cash/payroll/project permission surfaces, `src/demo/DemoWorkspace.tsx`, `src/lib/workspacePresentation.ts`, `src/utils/appRouteTarget.ts`, `src/utils/accessControl.ts`, Assistant registry/operation definitions, `scripts/workflow-map/context.ts`, `tests/*` regression additions, `docs/ENGORYX_CRUD_RBAC_MATRIX.md`, and this checkpoint.
- Known limitations: `.env.local` contains empty `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`; no authenticated session is available. Authenticated focus/visibility browser evidence, real Supabase/RLS/Storage replay, provider-email delivery, and production Assistant submit cannot be claimed locally.
- Handoff: PR [#43](https://github.com/Juvialski/InvoiceApp/pull/43) is open against `main` with title `Wave 4A: audit CRUD/RBAC and stabilize app navigation`; it has not been merged. Do not poll CI continuously from this local task. The final branch SHA and exact CI/live-validation boundary are reported in the task handoff.
