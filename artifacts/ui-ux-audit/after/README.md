# Local visual QA evidence

The final bounded demo visual run is described by `manifest.json` and `logs/qa.log`.
It covered 76 scenarios across 34 routes and four viewport profiles, with zero
console/page errors, failed requests, overflow failures, or failed scenarios.

The full screenshot set is retained in `screenshots/`. Representative captures
for handoff are copied to `../screenshots/after/`; these are demo evidence and
do not replace authenticated QA/provider evidence.
