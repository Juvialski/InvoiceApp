# Screenshot evidence

The `before` images are sanitized baseline/demo captures used to illustrate
the recorded friction; authenticated QA browser captures were inspected live
but are not persisted here when they could expose mailbox or company data.
The `after` images are local demo captures from the integrated feature branch.
The authenticated QA/provider results and their evidence boundary are recorded
in `../REPORT.md` and `../findings.json`.
